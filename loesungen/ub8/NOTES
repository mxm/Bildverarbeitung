d = x * cos alpha + y * sin alpha

imshow(I);

geraden zeichnen:
hold on
plot(vx, vy) <-- nimmt vektoren
hold off

kreise zeichnen:
r = 100;
for t = 0:0.1:2*pi
	plot (x + r * cos(t), y + r * sin(t));
